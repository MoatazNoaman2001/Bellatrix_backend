// import express from 'express';
// const router = express.Router();
// import { getConsulting, createConsulting, updateConsulting, deleteConsulting } from '../controllers/consultingController.js';
// import { authenticateToken } from '../middleware/authMiddleware.js';
// import { upload } from '../middleware/multerConfig.js';

// router.get('/', getConsulting);
// router.post('/', authenticateToken, upload.single('media'), createConsulting);
// router.patch('/', authenticateToken, upload.single('media'), updateConsulting);
// router.delete('/', authenticateToken, deleteConsulting);

// export default router; 